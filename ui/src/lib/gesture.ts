/**
 * The one press-to-drag threshold every pointer gesture shares: a press
 * that travels under it is a click (typing, the Band editor's reveal);
 * past it the gesture engages — the Scrub (#88), a Paint (#91).
 */
export const ENGAGE_PX = 3
