# Cross toolchain for the ARMv7 engine shim (`ddp-engine-arm`) — the
# same gcc the probe harness uses (`apt install gcc-arm-linux-gnueabihf`).
[target.armv7-unknown-linux-gnueabihf]
linker = "arm-linux-gnueabihf-gcc"
