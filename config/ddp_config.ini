[DDP]
; DolbyX daemon TCP port (must match dolbyx-daemon.py)
port=19876

; The settings below are only used by test_ddp.py, not the VST.
; The VST connects to the daemon which handles all DDP configuration.
sample_rate=48000
pre_gain_db=-6
post_gain_db=0
