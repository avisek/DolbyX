[package]
name = "ddp-vst-windows"
description = "Thin VST2 plugin for Windows hosts (EqualizerAPO) — audio over named pipe to the daemon"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]

[lints]
workspace = true
