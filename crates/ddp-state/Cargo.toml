[package]
name = "ddp-state"
description = "Pure state model: profiles, EQ presets, parameter metadata — no I/O"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true
