[package]
name = "ddp-engine"
description = "Engine seam: the Engine trait plus stub and QEMU backends"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
tracing.workspace = true

[features]
# Binds the integration tests to the real engine under qemu-arm-static
# (builds ddp-engine-arm + stages tools/ddp_probe's proven stub libs).
qemu = []
